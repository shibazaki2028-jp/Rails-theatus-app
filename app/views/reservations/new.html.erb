<h1>座席選択: <%= @schedule.movie.title %></h1>
<p>映画館：<%= @schedule.theater.name %></p>
<p>スクリーン：<%= @schedule.screen.info %></p>
<p>上映開始: <%= @schedule.screened_at.strftime("%Y-%m-%d %H:%M") %></p>

<div id="price-options-data" style="display: none;">
  <%= options_from_collection_for_select(@prices, :id, :name_with_value) %>
</div>

<% if current_account.general? || current_account.system_admin? %>
  <%= link_to "映画の詳細に戻る", movie_path(@schedule.movie) %>
<% else %>
  <%= link_to "映画館詳細に戻る", system_admin_theater_path(current_account.theater) %>
<% end %>

<%= form_with(model: [@schedule, @reservation], local: true) do |f| %>

  <div class="seat-selection">
    <% @seats.group_by(&:queue).each do |queue, seats_in_row| %>
      <div class="seat-row">
        <%= queue %>行:
        <% seats_in_row.each do |seat| %>
          <label class="seat-label">
            <% is_reserved = seat.reserved?(@schedule) %>
            <%= check_box_tag "seat_ids[]", seat.id, false, 
                disabled: is_reserved, 
                class: "seat-check",
                data: { seat_id: seat.id, seat_name: "#{queue}行-#{seat.verse}" }
            %>
            <span class="<%= 'reserved' if is_reserved %>"><%= seat.verse %></span>
          </label>
        <% end %>
      </div>
    <% end %>
  </div>



  <div id="selected-seats-list" class="selected-seats-container">
    <p class="no-selection">座席を選択してください</p>
  </div>

  <div class="actions" style="margin-top: 20px;">
    <%= f.submit "選択した座席を予約する", class: "btn-primary" %>
  </div>
<% end %>

