<% @page_title = "映画館管理(システム管理者)" %>
<h1><%= @page_title %></h1>

<%= link_to "新規映画館登録", new_system_admin_theater_path %>

<table>
  <thead>
    <tr>
      <th>映画館名</th>
      <th>住所</th>
      <th>電話番号</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <% @theaters.each do |theater| %>
      <tr>
        <td><%= theater.name %></td>
        <td><%= theater.address %></td>
        <td><%= theater.telephoen %></td>
        <td>
          <%= link_to "詳細", system_admin_theater_path(theater) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>